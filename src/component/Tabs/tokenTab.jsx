import React, { useState } from 'react'

const TokenTab = () => {

    const tokenTabs = [
        { name: "Token" },
        { name: "Change Password" },
        { name: "Logout" },
    ];

    return (
        <div>
            Token
        </div>
    );
}

export default TokenTab;
